<% unless @user == current_user %>
  <div id="follow_form">
    <% if current_user.following?(@user) %>
      <%= render 'unfollow_link' %>
    <% else %>
      <%= render 'follow_link' %>
    <% end %>
  </div>
<% end %>
